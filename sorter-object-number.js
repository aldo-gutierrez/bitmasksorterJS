import {radixBitSorterObjectNumber} from "./radix-bit-sorter-object-number.js";

export function sortObjectNumber(arrayObj, mapper, start, endP1) {
    return radixBitSorterObjectNumber(arrayObj, mapper, start, endP1);
}