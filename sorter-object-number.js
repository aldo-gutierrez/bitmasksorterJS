import {radixBitSorterObjectNumber} from "./radix-bit-sorter-object-number.js";

export function sortObjectNumber(arrayObj, mapper, options) {
    return radixBitSorterObjectNumber(arrayObj, mapper, options);
}